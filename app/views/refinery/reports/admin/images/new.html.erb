<%= form_for [refinery, :reports_admin, @image] do |f| %>
  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>
  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false %>
<% end %>

